// Movement here




// Targeting
target = scan_ships(self, team, enemy_team);


// Attack
if (target != noone) {
	targetX = target.x;
	targetY = target.y;
	
	if (alarm[0] == -1) alarm[0] = global.ship_fire_rate
}


// Die
if (hp < 0)
{
	instance_destroy();
}